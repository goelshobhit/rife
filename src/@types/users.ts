// ----------------------------------------------------------------------

export type usersState = {
  loading: Boolean;
  error: Boolean;
  usersList: Array<[]>;
  adminUsersList: Array<[]>;
  invitedUsersList: Array<[]>;
};
